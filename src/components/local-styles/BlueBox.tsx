import React, { ReactElement } from 'react';
import './bluebox.css';

const BlueBox = (): ReactElement => (
  <>
    <div>This is a blue box</div>
    <div>This is a circle box</div>
  </>
);

export default BlueBox;
